<!DOCTYPE html>
<html lang="it">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Profilo di <%= user.nome %> <%= user.cognome %></title>
    <link href="/stylesheet/Navbar.css" rel="stylesheet" type="text/css">

    <link href="/stylesheet/Footer.css" rel="stylesheet" type="text/css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.4/dist/css/bootstrap.min.css" rel="stylesheet">

    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/animate.css/4.1.1/animate.min.css"/>
    <script src="/javascript/Main.js" type="module" defer></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.4/dist/js/bootstrap.bundle.min.js"></script>
</head>
<body>
    <%- include('partials/navbar') %>
    
    <div class="profile-container">
        <div class="profile-card animate__animated animate__fadeIn overflow-hidden">
            <div class="profile-avatar mb-4 animate__animated animate__zoomIn d-flex justify-content-center align-items-center mt-4" style="min-height:140px;">
                <% if (imageUrl) { %>
                  <img src="<%= imageUrl %>" alt="Immagine profilo" class="rounded-circle" style="width: 120px; height: 120px; object-fit: cover; border: 2px solid #ccc;">
                <% } else { %>
                  <i class="bi bi-person-circle" style="font-size: 120px;"></i>
                <% } %>
            </div>
            <h2 class="text-center mb-1 animate__animated animate__fadeIn animate__delay-1s overflow-hidden"><%= user.nome %> <%= user.cognome %></h2>
            <p class="text-center text-muted mb-4 animate__animated animate__fadeIn animate__delay-1s">
                <i class="bi bi-envelope"></i> <%= user.email %>
            </p>
            
            <dl class="row profile-details animate__animated animate__fadeIn animate__delay-2s">
                <dt class="col-5"><i class="bi bi-telephone"></i> Telefono</dt>
                <dd class="col-7"><%= user.telefono ? user.telefono : 'Non specificato' %></dd>

                <dt class="col-5"><i class="bi bi-person-badge"></i> Tipo utente</dt>
                <dd class="col-7"><span class="badge bg-primary bg-opacity-10 text-primary px-2 py-1 rounded-pill">
                    <%= user.tipo_utente_nome ? user.tipo_utente_nome : 'Non specificato' %>
                </span></dd>

                <dt class="col-5"><i class="bi bi-calendar-check"></i> Registrato il</dt>
                <dd class="col-7"><%= user.data_registrazione ? user.data_registrazione.substring(0,10) : 'N/D' %></dd>
            </dl>
            
            <div class="text-center mt-4 animate__animated animate__fadeIn animate__delay-3s">
                <a href="/Logout" class="btn btn-outline-danger">
                    <i class="bi bi-box-arrow-right"></i> Esci
                </a>
            </div>
        </div>    
      </div>

    </div>


    <!-- Modal Modifica Profilo -->
<div class="modal fade m-auto p-auto" id="editProfileModal" tabindex="-1" aria-labelledby="editProfileModalLabel" aria-hidden="true">
  <div class="modal-dialog">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title" id="editProfileModalLabel">Modifica Profilo</h5>
        <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
      </div>
      <div class="modal-body">
        <form id="editProfileForm" autocomplete="off">
          <div class="mb-3">
            <label for="editNome" class="form-label">Nome</label>
            <input type="text" class="form-control" id="editNome" name="nome">
          </div>
          <div class="mb-3">
            <label for="editCognome" class="form-label">Cognome</label>
            <input type="text" class="form-control" id="editCognome" name="cognome">
          </div>
          <div class="mb-3">
            <label for="editEmail" class="form-label">Email</label>
            <input type="email" class="form-control" id="editEmail" name="email">
          </div>
          <div class="mb-3">
            <label for="editTelefono" class="form-label">Telefono</label>
            <input type="text" class="form-control" id="editTelefono" name="telefono">
          </div>
          <button type="submit" class="btn btn-success w-100">Salva modifiche</button>
        </form>
        <div id="editProfileMsg" class="mt-2"></div>
      </div>
    </div>
  </div>
</div>

    <div id="page"></div>
    <%- include('partials/footer.ejs') %>
</body>
</html>