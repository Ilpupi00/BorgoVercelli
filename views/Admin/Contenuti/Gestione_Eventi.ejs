<!DOCTYPE html>
<html lang="it">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Gestione Eventi - <%= user.nome %> <%= user.cognome %></title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.4/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/animate.css/4.1.1/animate.min.css"/>
    <script src="/javascripts/Main.js" type="module" defer></script>
    <script src="/javascripts/components/Admin.js" defer></script>
    <script src="/javascripts/components/GestioneEventi.js" defer></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.4/dist/js/bootstrap.bundle.min.js"></script>
    <link rel="stylesheet" href="/stylesheets/Admin.css">
</head>
<body>

    <div class="container-fluid">
        <div class="row">
            <!-- Sidebar -->
            <%- include('../../partials/sidebar.ejs'); %>

            <!-- Contenuto principale -->
            <div class="col-lg-9 col-xl-10 px-4 py-5">
                <div class="row justify-content-center">
                    <div class="col-12">

                        <!-- Header con profilo -->
                        <div class="admin-header animate__animated animate__fadeInDown mb-4">
                            <div class="row align-items-center">
                                <div class="col-md-8 text-center text-md-start">
                                    <h1 class="display-5 fw-bold text-dark mb-2">
                                        <i class="bi bi-calendar-event text-primary me-2"></i>
                                        Gestione Eventi
                                    </h1>
                                    <p class="text-primary fw-semibold mb-0">Amministratore del Sistema</p>
                                </div>
                                <div class="col-md-4 text-center text-md-end mt-3 mt-md-0">
                                    <a href="/admin" class="btn btn-outline-secondary btn-modern me-2">
                                        <i class="bi bi-arrow-left"></i> Torna al Pannello
                                    </a>
                                </div>
                            </div>
                        </div>

                        <!-- Contenuto principale -->
                        <div class="admin-content animate__animated animate__fadeInUp animate__delay-1s">
                            <div class="d-flex justify-content-between align-items-center mb-4">
                                <h2 class="mb-0">
                                    <i class="bi bi-list-ul text-primary me-2"></i>
                                    Tutti gli Eventi
                                </h2>
                                <a href="/crea-evento" class="btn btn-primary btn-modern">
                                <i class="bi bi-plus-circle me-2"></i>
                                    Crea Nuovo Evento
                                </a>
                            </div>

                            <!-- Barra di ricerca e filtri -->
                            <div class="card mb-4">
                                <div class="card-body">
                                    <div class="row g-3">
                                        <!-- Barra di ricerca -->
                                        <div class="col-md-6">
                                            <div class="input-group">
                                                <span class="input-group-text">
                                                    <i class="bi bi-search"></i>
                                                </span>
                                                <input type="text" class="form-control" id="searchInput" placeholder="Cerca per titolo o luogo...">
                                                <button class="btn btn-outline-secondary" type="button" id="clearSearch">
                                                    <i class="bi bi-x"></i>
                                                </button>
                                            </div>
                                        </div>

                                        <!-- Filtro stato -->
                                        <div class="col-md-2">
                                            <select class="form-select" id="statusFilter">
                                                <option value="all">Tutti gli stati</option>
                                                <option value="published">Pubblicati</option>
                                                <option value="draft">Bozze</option>
                                            </select>
                                        </div>

                                        <!-- Filtro tipo -->
                                        <div class="col-md-2">
                                            <select class="form-select" id="typeFilter">
                                                <option value="all">Tutti i tipi</option>
                                                <option value="Torneo">Torneo</option>
                                                <option value="Partita">Partita</option>
                                                <option value="Allenamento">Allenamento</option>
                                                <option value="Evento Sociale">Evento Sociale</option>
                                            </select>
                                        </div>

                                        <!-- Filtro data -->
                                        <div class="col-md-2">
                                            <select class="form-select" id="dateFilter">
                                                <option value="all">Tutte le date</option>
                                                <option value="upcoming">Futuri</option>
                                                <option value="past">Passati</option>
                                                <option value="today">Oggi</option>
                                                <option value="week">Questa settimana</option>
                                                <option value="month">Questo mese</option>
                                                <option value="year">Quest'anno</option>
                                            </select>
                                        </div>
                                    </div>

                                    <!-- Statistiche filtrate -->
                                    <div class="row mt-3">
                                        <div class="col-12">
                                            <small class="text-muted">
                                                <span id="totalCount"><%= eventi ? eventi.length : 0 %></span> eventi totali
                                                <span id="filteredCount" class="ms-3">(<%= eventi ? eventi.length : 0 %> filtrate)</span>
                                            </small>
                                        </div>
                                    </div>
                                </div>
                            </div>

                            <div class="card">
                                <div class="card-body">
                                    <div class="table-responsive">
                                        <table class="table table-hover">
                                            <thead class="table-dark">
                                                <tr>
                                                    <th>ID</th>
                                                    <th>Titolo</th>
                                                    <th>Data Inizio</th>
                                                    <th>Data Fine</th>
                                                    <th>Luogo</th>
                                                    <th>Tipo</th>
                                                    <th>Stato</th>
                                                    <th>Partecipanti Max</th>
                                                    <th>Azioni</th>
                                                </tr>
                                            </thead>
                                            <tbody id="eventiTableBody">
                                                <% if (eventi && eventi.length > 0) { %>
                                                    <% eventi.forEach(evento => { %>
                                                        <tr data-evento-id="<%= evento.id %>" 
                                                            data-titolo="<%= evento.titolo.toLowerCase() %>"
                                                            data-stato="<%= evento.pubblicato ? 'published' : 'draft' %>"
                                                            data-data-inizio="<%= evento.data_inizio || '' %>"
                                                            data-data-fine="<%= evento.data_fine || '' %>"
                                                            data-luogo="<%= (evento.luogo || '').toLowerCase() %>"
                                                            data-tipo="<%= evento.tipo_evento || '' %>">
                                                            <td><%= evento.id %></td>
                                                            <td>
                                                                <strong><%= evento.titolo %></strong>
                                                                <% if (evento.descrizione) { %>
                                                                    <br><small class="text-muted"><%= evento.descrizione.substring(0, 50) %>...</small>
                                                                <% } %>
                                                            </td>
                                                            <td><%= evento.data_inizio ? new Date(evento.data_inizio).toLocaleDateString('it-IT') : 'N/A' %></td>
                                                            <td><%= evento.data_fine ? new Date(evento.data_fine).toLocaleDateString('it-IT') : 'N/A' %></td>
                                                            <td><%= evento.luogo || 'N/A' %></td>
                                                            <td><%= evento.tipo_evento || 'N/A' %></td>
                                                            <td>
                                                                <% if (evento.pubblicato) { %>
                                                                    <span class="badge bg-success">Pubblicato</span>
                                                                <% } else { %>
                                                                    <span class="badge bg-warning">Bozza</span>
                                                                <% } %>
                                                            </td>
                                                            <td><%= evento.max_partecipanti || 'N/A' %></td>
                                                            <td>
                                                                <div class="btn-group" role="group">
                                                                    <a href="/evento/<%= evento.id %>" class="btn btn-sm btn-outline-primary" title="Visualizza">
                                                                        <i class="bi bi-eye"></i>
                                                                    </a>
                                                                    <button class="btn btn-sm btn-outline-warning btn-modifica-evento" title="Modifica" data-evento-id="<%= evento.id %>">
                                                                        <i class="bi bi-pencil"></i>
                                                                    </button>
                                                                    <button class="btn btn-sm btn-outline-success toggle-pubblicazione" title="<%= evento.pubblicato ? 'Sospendi' : 'Pubblica' %>" data-evento-id="<%= evento.id %>" data-current-status="<%= evento.pubblicato ? 1 : 0 %>">
                                                                        <i class="bi bi-<%= evento.pubblicato ? 'eye-slash' : 'eye' %>"></i>
                                                                    </button>
                                                                    <button class="btn btn-sm btn-outline-danger btn-elimina-evento" title="Elimina" data-evento-id="<%= evento.id %>">
                                                                        <i class="bi bi-trash"></i>
                                                                    </button>
                                                                </div>
                                                            </td>
                                                        </tr>
                                                    <% }); %>
                                                    <!-- Riga per nessun risultato dai filtri (inizialmente nascosta) -->
                                                    <tr id="noResultsRow" style="display: none;">
                                                        <td colspan="9" class="text-center text-muted py-4">
                                                            <i class="bi bi-search display-4 mb-3"></i>
                                                            <p>Nessun evento corrisponde ai filtri selezionati.</p>
                                                            <button class="btn btn-outline-secondary" onclick="gestioneEventi.clearAllFilters()">Rimuovi filtri</button>
                                                        </td>
                                                    </tr>
                                                <% } else { %>
                                                    <tr id="noResultsRow">
                                                        <td colspan="9" class="text-center text-muted py-4">
                                                            <i class="bi bi-info-circle display-4 mb-3"></i>
                                                            <p>Nessun evento presente nel sistema.</p>
                                                            <a href="/crea-evento" class="btn btn-primary">Crea il primo evento</a>
                                                        </td>
                                                    </tr>
                                                <% } %>
                                            </tbody>
                                        </table>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <div id="page"></div>

</body>
</html>
