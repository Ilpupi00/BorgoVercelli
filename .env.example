# Esempio variabili d'ambiente per Railway / produzione
# Railway fornisce una DATABASE_URL automaticamente quando si aggiunge il plugin Postgres
# Copia questo file in .env per sviluppo locale e sostituisci i valori

# URL di connessione Postgres (RFC-1738 style) — fornito da Railway in produzione
DATABASE_URL=postgres://user:password@host:5432/dbname

# Porta dell'app (Railway imposta automaticamente PORT)
PORT=3000

# Ambiente
NODE_ENV=development

# Opzionale: forzare SSL per Postgres
# PGSSLMODE=require
