<!DOCTYPE html>
<html lang="it">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Gestione Prenotazioni - Admin</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.4/dist/css/bootstrap.min.css" rel="stylesheet">
    <%- include('../../shared/views/partials/theme-includes') %>
    <link rel="stylesheet" href="/assets/styles/Admin.css">
    <link rel="stylesheet" href="/assets/styles/Admin_Global.css">
    <link rel="stylesheet" href="/assets/styles/theme-admin.css">
</head>
<body>

    <div class="container-fluid">
        <div class="row">
            <!-- Sidebar -->
            <%- include('partials/sidebar.ejs'); %>

            <!-- Contenuto principale -->
            <main class="col-lg-9 col-xl-10 px-4 py-5">
                <div class="admin-container">
                    <header class="admin-page-header slide-up">
                        <div class="admin-page-header-content">
                            <h1><i class="bi bi-calendar-check-fill"></i> Gestione Prenotazioni</h1>
                            <p class="admin-page-subtitle">Gestisci prenotazioni campi sportivi</p>
                        </div>
                        <div class="admin-page-header-actions">
                            <a href="/admin" class="btn btn-outline-secondary">
                                <i class="bi bi-arrow-left me-2"></i><span class="d-none d-sm-inline">Pannello</span>
                            </a>
                            <a href="/Logout" class="btn btn-outline-danger">
                                <i class="bi bi-box-arrow-right me-2"></i><span class="d-none d-sm-inline">Esci</span>
                            </a>
                        </div>
                    </header>

                    <section class="section-header fade-in">
                        <h2 class="section-title mb-0"><i class="bi bi-list-ul me-2"></i>Tutte le Prenotazioni</h2>
                        <div class="d-flex gap-2 flex-wrap">
                            <button class="btn btn-success btn-sm"><i class="bi bi-check-circle me-1"></i>Confermate</button>
                            <button class="btn btn-warning btn-sm"><i class="bi bi-clock me-1"></i>In Attesa</button>
                            <button class="btn btn-danger btn-sm"><i class="bi bi-x-circle me-1"></i>Annullate</button>
                            <button class="btn btn-secondary btn-sm"><i class="bi bi-calendar-x me-1"></i>Scadute</button>
                            <button id="deleteScaduteBtn" class="btn btn-outline-danger btn-sm">
                                <i class="bi bi-trash me-1"></i>Elimina Scadute
                            </button>
                        </div>
                    </section>

                    <section class="search-section fade-in">
                        <input type="text" id="searchInput" name="q" class="form-control form-control-lg" placeholder="🔍 Cerca prenotazioni...">
                    </section>

                    <section class="content-card fade-in">
                        <div class="table-responsive">
                            <table class="table admin-table">
                                <thead>
                                    <tr>
                                        <th>ID</th>
                                        <th>Campo</th>
                                        <th>Utente</th>
                                        <th>Squadra</th>
                                        <th>Data</th>
                                        <th>Orario</th>
                                        <th>Tipo Attività</th>
                                        <th>Stato</th>
                                        <th>Azioni</th>
                                    </tr>
                                </thead>
                                <tbody>
                                                <% if (prenotazioni && prenotazioni.length > 0) { %>
                                                    <% prenotazioni.forEach(prenotazione => { %>
                                                        <tr>
                                                            <td><%= prenotazione.id %></td>
                                                            <td><%= prenotazione.campo_nome || 'Campo ' + prenotazione.campo_id %></td>
                                                            <td>
                                                                <% if (prenotazione.utente_nome) { %>
                                                                    <%= prenotazione.utente_nome %> <%= prenotazione.utente_cognome %>
                                                                <% } else { %>
                                                                    -
                                                                <% } %>
                                                            </td>
                                                            <td><%= prenotazione.squadra_nome || '-' %></td>
                                                            <td><%= prenotazione.data_prenotazione ? new Date(prenotazione.data_prenotazione).toLocaleDateString('it-IT') : '-' %></td>
                                                            <td><%= prenotazione.ora_inizio %> - <%= prenotazione.ora_fine %></td>
                                                            <td><%= prenotazione.tipo_attivita || '-' %></td>
                                                            <td>
                                                                <% if (prenotazione.stato === 'confermata') { %>
                                                                    <span class="badge badge-success">Confermata</span>
                                                                <% } else if (prenotazione.stato === 'in_attesa') { %>
                                                                    <span class="badge badge-warning">In Attesa</span>
                                                                <% } else if (prenotazione.stato === 'annullata') { %>
                                                                    <span class="badge badge-danger">Annullata</span>
                                                                <% } else if (prenotazione.stato === 'scaduta') { %>
                                                                    <span class="badge badge-secondary">Scaduta</span>
                                                                <% } else { %>
                                                                    <span class="badge badge-secondary"><%= prenotazione.stato %></span>
                                                                <% } %>
                                                            </td>
                                                            <td>
                                                                <div class="btn-group" role="group">
                                                                    <button class="btn btn-sm btn-outline-primary" title="Visualizza" data-id="<%= prenotazione.id %>">
                                                                        <i class="bi bi-eye"></i>
                                                                    </button>
                                                                    <% if (prenotazione.stato === 'in_attesa') { %>
                                                                        <button class="btn btn-sm btn-outline-success" title="Conferma" data-id="<%= prenotazione.id %>">
                                                                            <i class="bi bi-check"></i>
                                                                        </button>
                                                                        <button class="btn btn-sm btn-outline-danger" title="Annulla" data-id="<%= prenotazione.id %>">
                                                                            <i class="bi bi-x"></i>
                                                                        </button>
                                                                    <% } else if (prenotazione.stato === 'confermata') { %>
                                                                        <button class="btn btn-sm btn-outline-warning" title="Modifica" data-id="<%= prenotazione.id %>">
                                                                            <i class="bi bi-pencil"></i>
                                                                        </button>
                                                                        <button class="btn btn-sm btn-outline-danger" title="Annulla" data-id="<%= prenotazione.id %>">
                                                                            <i class="bi bi-x"></i>
                                                                        </button>
                                                                    <% } else if (prenotazione.stato === 'annullata') { %>
                                                                        <button class="btn btn-sm btn-outline-success" title="Riattiva" data-id="<%= prenotazione.id %>">
                                                                            <i class="bi bi-arrow-counterclockwise"></i>
                                                                        </button>
                                                                    <% } %>
                                                                    <button class="btn btn-sm btn-outline-danger" title="Elimina" data-id="<%= prenotazione.id %>">
                                                                        <i class="bi bi-trash"></i>
                                                                    </button>
                                                                </div>
                                                            </td>
                                                        </tr>
                                                    <% }); %>
                                                <% } else { %>
                                                    <tr>
                                                        <td colspan="9" class="text-center text-muted py-4">
                                                            <i class="bi bi-info-circle display-4 mb-3"></i>
                                                            <p>Nessuna prenotazione presente nel sistema.</p>
                                                        </td>
                                                    </tr>
                                                <% } %>
                                            </tbody>
                                        </table>
                                    </div>
                                </section>
                            </main>
        </div>
    </div>

    <!-- Modal Dettagli Prenotazione -->
    <div class="modal fade" id="visualizzaPrenotazioneModal" tabindex="-1">
        <div class="modal-dialog modal-dialog-centered modal-lg">
            <div class="modal-content">
                <div class="modal-header bg-primary text-white">
                    <h5 class="modal-title"><i class="bi bi-calendar-check me-2"></i>Dettagli Prenotazione</h5>
                    <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal" aria-label="Close"></button>
                </div>
                <div class="modal-body" id="modalPrenotazioneBody">
                    <!-- Contenuto dinamico caricato da JS -->
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">
                        <i class="bi bi-x-circle me-2"></i>Chiudi
                    </button>
                </div>
            </div>
        </div>
    </div>

    <!-- Modal Conferma Eliminazione -->
    <div class="modal fade" id="deleteConfirmModal" tabindex="-1">
        <div class="modal-dialog modal-dialog-centered">
            <div class="modal-content">
                <div class="modal-header bg-danger text-white">
                    <h5 class="modal-title"><i class="bi bi-exclamation-triangle me-2"></i>Conferma Eliminazione</h5>
                    <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal" aria-label="Close"></button>
                </div>
                <div class="modal-body">
                    <p>Sei sicuro di voler eliminare questa prenotazione?</p>
                    <p class="text-muted mb-0">Questa azione non può essere annullata.</p>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">
                        <i class="bi bi-x-circle me-2"></i>Annulla
                    </button>
                    <button type="button" class="btn btn-danger" id="confirmDeleteBtn">
                        <i class="bi bi-trash me-2"></i>Elimina
                    </button>
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.4/dist/js/bootstrap.bundle.min.js"></script>
    <script src="/assets/scripts/components/AdminGlobal.js"></script>
    <script src="/assets/scripts/components/Admin.js"></script>
    <script src="/assets/scripts/components/AdminSidebar.js"></script>
    <script src="/assets/scripts/GestionePrenotazione.js"></script>
</body>
</html>
