<!DOCTYPE html>
<html lang="it">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Profilo di <%= user.nome %> <%= user.cognome %> - Borgo Vercelli</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.4/dist/css/bootstrap.min.css" rel="stylesheet">
    <%- include('../../shared/views/partials/theme-includes') %>
    <link rel="stylesheet" href="/assets/styles/scroll-animations.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/animate.css/4.1.1/animate.min.css"/>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <link rel="stylesheet" href="/assets/styles/Profilo.css">
    <link rel="stylesheet" href="/assets/styles/theme-profilo-light.css">
    <link rel="stylesheet" href="/assets/styles/Navbar.css">
    <link rel="stylesheet" href="/assets/styles/Footer.css">
    <link rel="stylesheet" href="/assets/styles/evento-upload.css">
    <link rel="stylesheet" href="/assets/styles/image-editor.css">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/cropperjs/1.6.1/cropper.min.css" rel="stylesheet">
    <script src="https://cdnjs.cloudflare.com/ajax/libs/cropperjs/1.6.1/cropper.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.4/dist/js/bootstrap.bundle.min.js"></script>
    <script src="/assets/scripts/image-editor-common.js"></script>
    <script src="/assets/scripts/Profilo.js" defer></script>
</head>
<body>
    <%- include('partials/navbar') %>

    <main class="profile-container">
        <!-- Header profilo con informazioni utente -->
        <header class="profile-header-card reveal reveal--fade-up">
            <div class="row align-items-center">
                <div class="col-md-4 text-center">
                    <div class="profile-avatar mb-3 reveal reveal--scale-up reveal--delay-1">
                        <div class="position-relative d-inline-block pt-2">
                            <% if (imageUrl) { %>
                                <img src="<%= imageUrl %>?t=<%= Date.now() %>" alt="Immagine profilo" class="rounded-circle profile-img-main" onerror="this.onerror=null; this.src='/assets/images/Campo.png';">
                            <% } else { %>
                                <i class="bi bi-person-circle profile-img-main"></i>
                            <% } %>
                            <div class="position-absolute bottom-0 end-0">
                                <% if (imageUrl) { %>
                                <button type="button" class="btn btn-success btn-sm rounded-circle p-2 shadow me-1" title="Modifica foto" onclick="window.openImageEditor('<%= imageUrl %>')">
                                    <span class="bi bi-scissors"></span>
                                </button>
                                <% } %>
                                <button id="editPicBtn" type="button" class="btn btn-primary btn-sm rounded-circle p-2 shadow" title="Cambia foto" data-bs-toggle="modal" data-bs-target="#uploadPicModal">
                                    <span class="bi bi-camera-fill"></span>
                                </button>
                            </div>
                        </div>
                    </div>
                    <div class="user-badges mt-2">
                        <% if (user.tipo_utente_id === 1) { %>
                            <span class="badge bg-danger me-1 mb-1"><i class="bi bi-shield-check"></i> Admin</span>
                        <% } else if (user.tipo_utente_id === 2) { %>
                            <span class="badge bg-primary me-1 mb-1"><i class="bi bi-person-badge"></i> Presidente</span>
                        <% } else if (user.tipo_utente_id === 3) { %>
                            <span class="badge bg-success me-1 mb-1"><i class="bi bi-person-badge-fill"></i> Vice Presidente</span>
                        <% } else if (user.tipo_utente_id === 4) { %>
                            <span class="badge bg-warning text-dark me-1 mb-1"><i class="bi bi-briefcase"></i> Dirigente</span>
                        <% } else if (user.tipo_utente_id === 5) { %>
                            <span class="badge bg-secondary me-1 mb-1"><i class="bi bi-file-earmark-text"></i> Segretario</span>
                        <% } else { %>
                            <span class="badge bg-info me-1 mb-1"><i class="bi bi-person-check"></i> Utente</span>
                        <% } %>
                        <% if (stats && stats.prenotazioni_totali > 10) { %>
                            <span class="badge bg-warning text-dark mb-1"><i class="bi bi-trophy"></i> Giocatore Attivo</span>
                        <% } %>
                    </div>
                      <!-- Contact lines moved here (left column) -->
                      <div class="contact-lines mt-3 text-start">
                        <p class="text-white mb-1 fs-6">
                          <i class="bi bi-envelope me-2"></i>
                          <a href="mailto:<%= user.email %>" class="text-white text-decoration-underline"><%= user.email %></a>
                        </p>
                        <% if (user.telefono) { %>
                        <p class="text-white mb-1 fs-6">
                          <i class="bi bi-telephone me-2"></i>
                          <span><%= user.telefono %></span>
                        </p>
                        <% } %>
                        <% if (user.data_nascita) { %>
                        <p class="text-white mb-1 fs-6">
                          <i class="bi bi-calendar3 me-2"></i>
                          <span><%= new Date(user.data_nascita).toLocaleDateString('it-IT') %></span>
                        </p>
                        <% } %>
                        <% if (user.codice_fiscale) { %>
                        <p class="text-white mb-0 fs-6">
                          <i class="bi bi-card-text me-2"></i>
                          <span><%= user.codice_fiscale.toUpperCase() %></span>
                        </p>
                        <% } %>
                      </div>
                </div>
                <div class="col-md-8">
                    <div class="profile-info">
                        <h1 class="display-5 fw-bold text-white mb-2"><%= user.nome %> <%= user.cognome %></h1>
                        <!-- contact-lines moved to left column to keep layout consistent -->
                        <div class="member-since">
                            <i class="bi bi-calendar-check text-white me-2"></i>
                            <span class="text-white">Utente dal <%= user.data_registrazione ? new Date(user.data_registrazione).toLocaleDateString('it-IT', { year: 'numeric', month: 'long' }) : 'N/D' %></span>
                        </div>
                    </div>
                </div>
            </div>
        </header>

        <div class="row mt-4">
            <!-- Sezione sinistra: Statistiche e Link Rapidi -->
            <aside class="col-lg-4">
                <!-- Card Statistiche -->
                <div class="card stats-card reveal reveal--slide-left">
                    <div class="card-header bg-primary text-white">
                        <h5 class="card-title mb-0">
                            <i class="bi bi-bar-chart-line me-2"></i>Le tue Statistiche
                        </h5>
                    </div>
                    <div class="card-body">
                        <div class="stat-item">
                            <div class="stat-icon">
                                <i class="bi bi-calendar-check-fill text-success"></i>
                            </div>
                            <div class="stat-content">
                                <div class="stat-number"><%= stats.prenotazioni_totali || 0 %></div>
                                <div class="stat-label">Prenotazioni Totali</div>
                            </div>
                        </div>
                        <div class="stat-item">
                            <div class="stat-icon">
                                <i class="bi bi-star-fill text-warning"></i>
                            </div>
                            <div class="stat-content">
                                <div class="stat-number"><%= stats.recensioni_totali || 0 %></div>
                                <div class="stat-label">Recensioni Scritte</div>
                            </div>
                        </div>
                        <div class="stat-item">
                            <div class="stat-icon">
                                <i class="bi bi-calendar-event text-info"></i>
                            </div>
                            <div class="stat-content">
                                <div class="stat-number"><%= stats.prenotazioni_mese || 0 %></div>
                                <div class="stat-label">Questo Mese</div>
                            </div>
                        </div>
                    </div>
                </div>

                <!-- Card Link Rapidi -->
                <div class="card quick-links-card reveal reveal--slide-left reveal--delay-1 mt-3">
                    <div class="card-header bg-success text-white">
                        <h5 class="card-title mb-0">
                            <i class="bi bi-lightning-charge me-2"></i>Azioni Rapide
                        </h5>
                    </div>
                    <div class="card-body">
                        <button class="btn btn-outline-primary w-100 mb-2" onclick="modificaPreferenze()">
                            <i class="bi bi-pencil me-2"></i>Modifica Profilo
                        </button>
                    <button id="enablePushBtn" class="btn btn-outline-success w-100 mb-2">
                      <i class="bi bi-bell-fill me-2"></i>Abilita Notifiche
                    </button>
                        <a href="/users/mie-prenotazioni" class="btn btn-outline-info w-100 mb-2">
                            <i class="bi bi-calendar-check me-2"></i>Le Mie Prenotazioni
                        </a>
                        <a href="/prenotazione" class="btn btn-outline-success w-100 mb-2">
                            <i class="bi bi-calendar-plus me-2"></i>Prenota Campo
                        </a>
                        <a href="/eventi" class="btn btn-outline-primary w-100 mb-2">
                            <i class="bi bi-calendar-event me-2"></i>Vedi Eventi
                        </a>
                        <a href="/scrivi/Recensione" class="btn btn-outline-warning w-100 mb-2">
                            <i class="bi bi-star me-2"></i>Scrivi Recensione
                        </a>
                        <a href="/galleria" class="btn btn-outline-info w-100 mb-2">
                            <i class="bi bi-images me-2"></i>Galleria Foto
                        </a>
                        <a href="/Logout" class="btn btn-outline-danger w-100">
                            <i class="bi bi-box-arrow-right me-2"></i>Logout
                        </a>
                    </div>
                </div>

                <!-- Card Preferenze Sportive -->
                <div class="card preferences-card reveal reveal--slide-left reveal--delay-2 mt-3">
                    <div class="card-header bg-info text-white">
                        <h5 class="card-title mb-0">
                            <i class="bi bi-gear me-2"></i>Preferenze e Sicurezza
                        </h5>
                    </div>
                    <div class="card-body">
                        <div class="row">
                            <div class="col-12 mb-3">
                                <button class="btn btn-outline-secondary w-100 mb-2" onclick="cambiaPassword()">
                                    <i class="bi bi-key me-2"></i>Cambia Password
                                </button>
                            </div>
                        </div>
                        <div class="preference-item">
                            <small class="text-muted">Ruolo preferito:</small>
                            <div class="fw-bold"><%= user.ruolo_preferito || 'Non specificato' %></div>
                        </div>
                        <div class="preference-item">
                            <small class="text-muted">Piede preferito:</small>
                            <div class="fw-bold"><%= user.piede_preferito || 'Non specificato' %></div>
                        </div>
                    </div>
                </div>

        

            </aside>

            <!-- Sezione destra: Attività e Dettagli -->
            <section class="col-lg-8">
                <!-- Card Attività Recente -->
                <div class="card activity-card reveal reveal--slide-right">
                    <div class="card-header bg-warning text-dark">
                        <h5 class="card-title mb-0">
                            <i class="bi bi-activity me-2"></i>Attività Recente
                        </h5>
                    </div>
                    <div class="card-body">
                        <% if ((activity.prenotazioni && activity.prenotazioni.length > 0) || (activity.recensioni && activity.recensioni.length > 0)) { %>
                            <div class="activity-timeline">
                                <% (activity.prenotazioni || []).slice(0, 2).forEach(prenotazione => { %>
                                    <div class="activity-item">
                                        <div class="activity-icon bg-success">
                                            <i class="bi bi-calendar-check"></i>
                                        </div>
                                        <div class="activity-content">
                                            <div class="activity-title">Prenotazione Campo</div>
                                            <div class="activity-details">
                                                <%= prenotazione.campo_nome || 'Campo' %> -
                                                <%= new Date(prenotazione.data_prenotazione).toLocaleDateString('it-IT') %> alle <%= prenotazione.ora_inizio %>
                                            </div>
                                            <small class="text-muted">
                                                <%= new Date(prenotazione.created_at).toLocaleDateString('it-IT') %>
                                            </small>
                                        </div>
                                    </div>
                                <% }); %>

                                <% (activity.recensioni || []).slice(0, 2).forEach(recensione => { %>
                                    <div class="activity-item">
                                        <div class="activity-icon bg-warning">
                                            <i class="bi bi-star"></i>
                                        </div>
                                        <div class="activity-content">
                                            <div class="activity-title">Recensione Scritta</div>
                                            <div class="activity-details">
                                                "<%= recensione.titolo %>" - <%= '★'.repeat(recensione.valutazione) %>
                                            </div>
                                            <small class="text-muted">
                                                <%= new Date(recensione.created_at).toLocaleDateString('it-IT') %>
                                            </small>
                                        </div>
                                    </div>
                                <% }); %>
                            </div>
                        <% } else { %>
                            <div class="text-center py-4">
                                <i class="bi bi-info-circle display-4 text-muted mb-3"></i>
                                <p class="text-muted">Nessuna attività recente da mostrare.</p>
                                <p class="text-muted small">Le tue attività appariranno qui quando prenoterai campi o scriverai recensioni.</p>
                            </div>
                        <% } %>
                    </div>
                </div>

                <!-- Card Informazioni Sportive -->
                <% if (giocatore) { %>
                <div class="card sports-card reveal reveal--slide-right reveal--delay-1 mt-3">
                    <div class="card-header bg-success text-white">
                        <h5 class="card-title mb-0">
                            <i class="bi bi-trophy me-2"></i>Informazioni Sportive
                        </h5>
                    </div>
                    <div class="card-body">
                        <div class="row">
                            <div class="col-md-6">
                                <div class="sports-info">
                                    <i class="bi bi-person-badge text-success me-2"></i>
                                    <strong>Ruolo:</strong> <%= giocatore.ruolo || 'Non specificato' %>
                                </div>
                            </div>
                            <div class="col-md-6">
                                <div class="sports-info">
                                    <i class="bi bi-hash text-success me-2"></i>
                                    <strong>Numero Maglia:</strong> <%= giocatore.numero_maglia || 'N/A' %>
                                </div>
                            </div>
                        </div>
                        <div class="row mt-2">
                            <div class="col-md-6">
                                <div class="sports-info">
                                    <i class="bi bi-calendar-event text-success me-2"></i>
                                    <strong>Tesseramento:</strong> <%= giocatore.data_inizio_tesseramento ? new Date(giocatore.data_inizio_tesseramento).getFullYear() : 'N/D' %>
                                </div>
                            </div>
                            <div class="col-md-6">
                                <div class="sports-info">
                                    <i class="bi bi-flag text-success me-2"></i>
                                    <strong>Nazionalità:</strong> <%= giocatore.nazionalita || 'Non specificata' %>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
                <% } %>

                <!-- Card Ruolo Dirigenziale -->
                <% if (user.tipo_utente_id >= 1) { %>
                <div class="card management-card reveal reveal--slide-right reveal--delay-2 mt-3">
                    <div class="card-header bg-warning text-dark">
                        <h5 class="card-title mb-0">
                            <i class="bi bi-briefcase me-2"></i>Ruolo Dirigenziale
                        </h5>
                    </div>
                    <div class="card-body">
                        <div class="row">
                            <div class="col-md-6">
                                <div class="management-info">
                                    <i class="bi bi-building text-warning me-2"></i>
                                    <strong>Squadra:</strong> <%= dirigente?.squadra_nome || 'Societario' %>
                                </div>
                            </div>
                            <div class="col-md-6">
                                <div class="management-info">
                                    <i class="bi bi-person-badge text-warning me-2"></i>
                                    <strong>Ruolo:</strong> <%= dirigente?.ruolo || 'Non specificato' %>
                                </div>
                            </div>
                        </div>
                        <div class="row mt-2">
                            <div class="col-md-6">
                                <div class="management-info">
                                    <i class="bi bi-calendar-check text-warning me-2"></i>
                                    <strong>Nomina:</strong> <%= dirigente?.data_nomina ? new Date(dirigente.data_nomina).toLocaleDateString('it-IT') : 'Non specificata' %>
                                </div>
                            </div>
                            <div class="col-md-6">
                                <div class="management-info">
                                    <i class="bi bi-calendar-x text-warning me-2"></i>
                                    <strong>Scadenza:</strong> <%= dirigente?.data_scadenza ? new Date(dirigente.data_scadenza).toLocaleDateString('it-IT') : 'Non specificata' %>
                                </div>
                            </div>
                        </div>
                        <div class="row justify-content-center align-center">
                          <div class="col-md-6 text-center text-white mt-3 gestione_bottone">
                            <% if (dirigente && dirigente.squadra_id) { %>
                              <a href="/squadre/gestione/<%= dirigente.squadra_id %>" class="btn btn-warning text-white">Gestione Squadra</a>
                            <% } else { %>
                              <span class="text-muted">Nessuna squadra assegnata</span>
                            <% } %>
                          </div>
                        </div>
                    </div>
                </div>
                <div class="card actions-card reveal reveal--slide-right reveal--delay-3 mt-3">
                  <div class="card-header bg-gradient-primary text-white">
                    <h5 class="card-title mb-0">
                      <i class="bi bi-plus-circle-fill me-2"></i>Gestione Contenuti
                    </h5>
                  </div>
                  <div class="card-body">
                    <p class="text-muted mb-4">Come dirigente, puoi creare e gestire eventi e notizie per la comunità.</p>
                    <div class="row g-3">
                      <div class="col-md-6">
                        <a href="/evento/crea-evento" class="btn btn-gradient-event w-100 py-3 position-relative overflow-hidden">
                          <div class="d-flex flex-column align-items-center">
                            <i class="bi bi-calendar-plus-fill fs-2 mb-2"></i>
                            <span class="fw-bold">Crea Nuovo Evento</span>
                            <small class="opacity-75">Organizza attività per la squadra</small>
                          </div>
                          <div class="position-absolute top-0 end-0 bg-primary bg-opacity-90 rounded-start-pill px-2 py-1">
                            <i class="bi bi-chevron-right text-white"></i>
                          </div>
                        </a>
                      </div>
                      <div class="col-md-6">
                        <a href="/notizie/crea_notizie" class="btn btn-gradient-news w-100 py-3 position-relative overflow-hidden">
                          <div class="d-flex flex-column align-items-center">
                            <i class="bi bi-newspaper fs-2 mb-2"></i>
                            <span class="fw-bold">Crea Nuova Notizia</span>
                            <small class="opacity-75">Condividi aggiornamenti importanti</small>
                          </div>
                          <div class="position-absolute top-0 end-0 bg-primary bg-opacity-90 rounded-start-pill px-2 py-1">
                            <i class="bi bi-chevron-right text-white"></i>
                          </div>
                        </a>
                      </div>
                    </div>
                    <div class="text-center mt-3">
                      <small class="text-muted">
                        <i class="bi bi-info-circle me-1"></i>
                        Tutti i contenuti verranno pubblicati dopo la revisione
                      </small>
                    </div>
                  </div>
                </div>
                <% } %>

                <!-- Card Le Mie Recensioni -->
                <div class="card reviews-card reveal reveal--fade-up mt-3">
                    <div class="card-header bg-info text-white">
                        <h5 class="card-title mb-0">
                            <i class="bi bi-star me-2"></i>Le Mie Recensioni
                        </h5>
                    </div>
                    <div class="card-body">
                        <div id="userReviewsContainer">
                            <div class="text-center py-3">
                                <div class="spinner-border text-info" role="status">
                                    <span class="visually-hidden">Caricamento...</span>
                                </div>
                                <p class="text-muted mt-2">Caricamento recensioni...</p>
                            </div>
                        </div>
                    </div>
                </div>

                <!-- Card Le Mie Notizie ed Eventi -->
                <% if (user.tipo_utente_id >= 1) { %>
                <div class="card news-events-card reveal reveal--fade-up reveal--delay-1 mt-3">
                    <div class="card-header bg-success text-white d-flex justify-content-between align-items-center">
                        <h5 class="card-title mb-0">
                            <i class="bi bi-newspaper me-2"></i>I Miei Contenuti
                        </h5>
                        <span class="badge bg-light text-dark" id="contentCount">0 elementi</span>
                    </div>
                    <div class="card-body">
                        <div id="userNewsEventsContainer">
                            <% if ((notiziePersonali && notiziePersonali.length > 0) || (eventiPersonali && eventiPersonali.length > 0)) { %>
                                <div class="news-events-list">
                                    <% (notiziePersonali || []).forEach(notizia => { %>
                                        <div class="news-event-item d-flex align-items-center justify-content-between p-3 border-bottom hover-shadow">
                                            <div class="d-flex align-items-center flex-grow-1">
                                                <div class="news-event-icon bg-primary text-white rounded-circle d-flex align-items-center justify-content-center me-3">
                                                    <i class="bi bi-newspaper fs-5"></i>
                                                </div>
                                                <div class="flex-grow-1">
                                                    <h6 class="mb-1 text-truncate"><%= notizia.titolo %></h6>
                                                    <p class="mb-1 text-muted small text-truncate"><%= notizia.sottotitolo || ((notizia.contenuto || '').replace(/<[^>]*>/g, '').substring(0, 80) + (notizia.contenuto && notizia.contenuto.length > 80 ? '...' : '')) %></p>
                                                    <small class="text-muted">
                                                        <i class="bi bi-calendar me-1"></i>
                                                        <%= notizia.data_pubblicazione ? new Date(notizia.data_pubblicazione).toLocaleDateString('it-IT') : 'Data non disponibile' %>
                                                        <% if (notizia.pubblicata === 1) { %>
                                                            <span class="badge bg-success ms-2">Pubblicato</span>
                                                        <% } else { %>
                                                            <span class="badge bg-warning ms-2">Bozza</span>
                                                        <% } %>
                                                    </small>
                                                </div>
                                            </div>
                                            <div class="btn-group" role="group">
                                                <% if (notizia.id || notizia.N_id) { %>
                                                    <a href="/notizie/edit/<%= notizia.id || notizia.N_id %>" class="btn btn-sm btn-outline-primary" title="Modifica notizia">
                                                        <i class="bi bi-pencil"></i>
                                                    </a>
                                                    <button class="btn btn-sm btn-outline-info" title="Visualizza" onclick="window.open('/notizia/<%= notizia.id || notizia.N_id %>', '_blank')">
                                                        <i class="bi bi-eye"></i>
                                                    </button>
                                                <% } else { %>
                                                    <button class="btn btn-sm btn-outline-secondary" disabled title="ID notizia non valido">
                                                        <i class="bi bi-eye-slash"></i>
                                                    </button>
                                                <% } %>
                                            </div>
                                        </div>
                                    <% }); %>

                                    <% (eventiPersonali || []).forEach(evento => { %>
                                        <div class="news-event-item d-flex align-items-center justify-content-between p-3 border-bottom hover-shadow">
                                            <div class="d-flex align-items-center flex-grow-1">
                                                <div class="news-event-icon bg-success text-white rounded-circle d-flex align-items-center justify-content-center me-3">
                                                    <i class="bi bi-calendar-event fs-5"></i>
                                                </div>
                                                <div class="flex-grow-1">
                                                    <h6 class="mb-1 text-truncate"><%= evento.titolo %></h6>
                                                    <p class="mb-1 text-muted small text-truncate"><%= evento.descrizione ? ((evento.descrizione || '').replace(/<[^>]*>/g, '').substring(0, 80) + (evento.descrizione && evento.descrizione.length > 80 ? '...' : '')) : 'Nessuna descrizione' %></p>
                                                    <small class="text-muted">
                                                        <i class="bi bi-calendar me-1"></i>
                                                        <%= evento.data_inizio ? new Date(evento.data_inizio).toLocaleDateString('it-IT') : 'Data non disponibile' %>
                                                        <% if (evento.ora_inizio) { %>
                                                            <i class="bi bi-clock me-1 ms-2"></i><%= evento.ora_inizio %>
                                                        <% } %>
                                                        <% if (evento.pubblicato === 1 || evento.pubblicato === true) { %>
                                                            <span class="badge bg-success ms-2">Pubblicato</span>
                                                        <% } else { %>
                                                            <span class="badge bg-warning ms-2">Bozza</span>
                                                        <% } %>
                                                    </small>
                                                </div>
                                            </div>
                                            <div class="btn-group" role="group">
                                                <a href="/evento/crea-evento/<%= evento.id %>" class="btn btn-sm btn-outline-success" title="Modifica evento">
                                                    <i class="bi bi-pencil"></i>
                                                </a>
                                                <button class="btn btn-sm btn-outline-info" title="Visualizza" onclick="window.open('/evento/<%= evento.id %>', '_blank')">
                                                    <i class="bi bi-eye"></i>
                                                </button>
                                            </div>
                                        </div>
                                    <% }); %>
                                </div>
                            <% } else { %>
                                <div class="text-center py-5">
                                    <div class="empty-state-icon mb-3">
                                        <i class="bi bi-plus-circle text-muted"></i>
                                    </div>
                                    <h6 class="text-muted mb-3">Nessun contenuto creato</h6>
                                    <p class="text-muted small mb-4">Inizia creando il tuo primo evento o notizia per la comunità.</p>
                                    <div class="d-flex justify-content-center gap-2">
                                        <a href="/evento/crea-evento" class="btn btn-outline-success">
                                            <i class="bi bi-calendar-plus me-2"></i>Crea Evento
                                        </a>
                                        <a href="/notizie/crea_notizie" class="btn btn-outline-primary">
                                            <i class="bi bi-newspaper me-2"></i>Crea Notizia
                                        </a>
                                    </div>
                                </div>
                            <% } %>
                        </div>
                    </div>
                </div>
                <% } %>
            </section>
        </div>
    </main>


    <!-- Modal Modifica Profilo -->
<div class="modal fade" id="editProfileModal" tabindex="-1" aria-labelledby="editProfileModalLabel" aria-hidden="true">
  <div class="modal-dialog modal-lg">
    <div class="modal-content">
      <div class="modal-header bg-primary text-white">
        <h5 class="modal-title" id="editProfileModalLabel">
          <i class="bi bi-pencil me-2"></i>Modifica Profilo
        </h5>
        <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal" aria-label="Close"></button>
      </div>
      <div class="modal-body">
        <form id="editProfileForm">
          <div class="row">
            <div class="col-md-6 mb-3">
              <label for="editNome" class="form-label">Nome</label>
              <input type="text" class="form-control" id="editNome" name="nome" value="<%= user.nome %>" autocomplete="given-name">
            </div>
            <div class="col-md-6 mb-3">
              <label for="editCognome" class="form-label">Cognome</label>
              <input type="text" class="form-control" id="editCognome" name="cognome" value="<%= user.cognome %>" autocomplete="family-name">
            </div>
          </div>
          <div class="mb-3">
            <label for="editEmail" class="form-label">Email</label>
            <input type="email" class="form-control" id="editEmail" name="email" value="<%= user.email %>" autocomplete="email">
          </div>
          <div class="mb-3">
            <label for="editTelefono" class="form-label">Telefono <span class="text-danger">*</span></label>
            <input type="tel" class="form-control" id="editTelefono" name="telefono" value="<%= user.telefono || '' %>" 
                   placeholder="+39 3xx xxxxxxx" 
                   pattern="^\+39\s*[0-9]{9,10}$"
                   title="Formato: +39 seguito da 9-10 cifre"
                   required autocomplete="tel">
            <div class="form-text"><i class="bi bi-info-circle me-1"></i>Formato: +39 3331234567 (con o senza spazi)</div>
            <div class="invalid-feedback">Inserisci un numero valido: +39 seguito da 9-10 cifre</div>
          </div>
          <div class="row">
            <div class="col-md-6 mb-3">
              <label for="editDataNascita" class="form-label">Data di Nascita</label>
              <input type="date" class="form-control" id="editDataNascita" name="data_nascita" 
                     value="<%= user.data_nascita ? new Date(user.data_nascita).toISOString().split('T')[0] : '' %>" 
                     max="<%= new Date().toISOString().split('T')[0] %>"
                     autocomplete="bday">
              <div class="form-text"><i class="bi bi-info-circle me-1"></i>Opzionale</div>
            </div>
            <div class="col-md-6 mb-3">
              <label for="editCodiceFiscale" class="form-label">Codice Fiscale</label>
              <input type="text" class="form-control text-uppercase" id="editCodiceFiscale" name="codice_fiscale" 
                     value="<%= user.codice_fiscale || '' %>" 
                     maxlength="16"
                     pattern="^[A-Z]{6}[0-9]{2}[A-Z][0-9]{2}[A-Z][0-9]{3}[A-Z]$"
                     placeholder="ES: RSSMRA80A01H501U"
                     style="text-transform: uppercase;">
              <div class="form-text"><i class="bi bi-info-circle me-1"></i>16 caratteri - Opzionale</div>
              <div class="invalid-feedback">Codice fiscale non valido (16 caratteri)</div>
            </div>
          </div>
          <div class="border-top pt-3">
            <h6 class="text-muted mb-3">Preferenze Sportive</h6>
            <div class="row">
              <div class="col-md-6 mb-3">
                <label for="ruoloPreferito" class="form-label">Ruolo Preferito</label>
                <select class="form-select" id="ruoloPreferito" name="ruolo_preferito">
                  <option value="">Seleziona ruolo</option>
                  <option value="portiere">Portiere</option>
                  <option value="difensore">Difensore</option>
                  <option value="centrocampista">Centrocampista</option>
                  <option value="attaccante">Attaccante</option>
                </select>
              </div>
              <div class="col-md-6 mb-3">
                <label for="piedePreferito" class="form-label">Piede Preferito</label>
                <select class="form-select" id="piedePreferito" name="piede_preferito">
                  <option value="">Seleziona piede</option>
                  <option value="destro">Destro</option>
                  <option value="sinistro">Sinistro</option>
                  <option value="ambidestro">Ambidestro</option>
                </select>
              </div>
            </div>
          </div>
          <div class="d-flex justify-content-end gap-2">
            <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Annulla</button>
            <button type="submit" class="btn btn-primary">
              <i class="bi bi-check-lg me-2"></i>Salva modifiche
            </button>
          </div>
        </form>
        <div id="editProfileMsg" class="mt-2"></div>
      </div>
    </div>
  </div>
</div>

    <div id="page"></div>

<!-- Modal upload foto profilo -->
<div class="modal fade" id="uploadPicModal" tabindex="-1" aria-labelledby="uploadPicModalLabel" aria-hidden="true">
  <div class="modal-dialog modal-dialog-centered">
    <div class="modal-content">
      <div class="modal-header bg-primary text-white">
        <h5 class="modal-title" id="uploadPicModalLabel">
          <i class="bi bi-camera me-2"></i>Carica nuova foto profilo
        </h5>
        <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal" aria-label="Close"></button>
      </div>
      <div class="modal-body d-flex flex-column align-items-center">
        <div class="text-center mb-3">
          <i class="bi bi-image display-4 text-muted mb-3"></i>
          <p class="text-muted">Seleziona una foto dal tuo dispositivo</p>
        </div>
        <form id="profilePicForm" enctype="multipart/form-data" class="w-100">
          <input type="file" id="profilePicInput" name="profilePic" accept="image/*" class="form-control mb-3">
          <div class="d-flex gap-2">
            <button type="button" class="btn btn-secondary flex-fill" data-bs-dismiss="modal">Annulla</button>
            <button type="submit" class="btn btn-primary flex-fill" id="uploadPicBtn">
              <i class="bi bi-upload me-2"></i>Carica Foto
            </button>
          </div>
          <div id="uploadPicMsg" class="mt-2"></div>
        </form>
      </div>
    </div>
  </div>
    </div>

<!-- Modal cambio password -->
<div class="modal fade" id="changePasswordModal" tabindex="-1" aria-labelledby="changePasswordModalLabel" aria-hidden="true">
  <div class="modal-dialog modal-dialog-centered">
    <div class="modal-content">
      <div class="modal-header bg-info text-white">
        <h5 class="modal-title" id="changePasswordModalLabel">
          <i class="bi bi-key me-2"></i>Cambia Password
        </h5>
        <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal" aria-label="Close"></button>
      </div>
      <div class="modal-body">
        <form id="changePasswordForm">
          <div class="mb-3">
            <label for="currentPassword" class="form-label">Password Attuale</label>
            <input type="password" class="form-control" id="currentPassword" name="currentPassword" required autocomplete="current-password">
            <div class="form-text">Inserisci la tua password attuale per confermare l'identità.</div>
          </div>
          <div class="mb-3">
            <label for="newPassword" class="form-label">Nuova Password</label>
            <input type="password" class="form-control" id="newPassword" name="newPassword" required minlength="6" autocomplete="new-password">
            <div class="form-text">La password deve essere di almeno 6 caratteri.</div>
          </div>
          <div class="mb-3">
            <label for="confirmPassword" class="form-label">Conferma Nuova Password</label>
            <input type="password" class="form-control" id="confirmPassword" name="confirmPassword" required minlength="6" autocomplete="new-password">
            <div class="form-text">Ripeti la nuova password per conferma.</div>
          </div>
          <div class="d-flex justify-content-end gap-2">
            <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Annulla</button>
            <button type="submit" class="btn btn-info">
              <i class="bi bi-check-lg me-2"></i>Cambia Password
            </button>
          </div>
        </form>
        <div id="changePasswordMsg" class="mt-2"></div>
      </div>
    </div>
  </div>
</div>

<!-- Modal modifica recensione -->
<div class="modal fade" id="editReviewModal" tabindex="-1" aria-labelledby="editReviewModalLabel" aria-hidden="true">
  <div class="modal-dialog modal-lg">
    <div class="modal-content">
      <div class="modal-header bg-warning text-dark">
        <h5 class="modal-title" id="editReviewModalLabel">
          <i class="bi bi-pencil me-2"></i>Modifica Recensione
        </h5>
        <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
      </div>
      <div class="modal-body">
        <form id="editReviewForm">
          <input type="hidden" id="editReviewId">
          <div class="mb-3">
            <label for="editReviewRating" class="form-label">Valutazione</label>
            <select class="form-select" id="editReviewRating" name="valutazione" required>
              <option value="1">⭐ - Molto insoddisfatto</option>
              <option value="2">⭐⭐ - Insoddisfatto</option>
              <option value="3">⭐⭐⭐ - Nella media</option>
              <option value="4">⭐⭐⭐⭐ - Soddisfatto</option>
              <option value="5">⭐⭐⭐⭐⭐ - Molto soddisfatto</option>
            </select>
          </div>
          <div class="mb-3">
            <label for="editReviewTitle" class="form-label">Titolo</label>
            <input type="text" class="form-control" id="editReviewTitle" name="titolo" required>
          </div>
          <div class="mb-3">
            <label for="editReviewContent" class="form-label">Contenuto</label>
            <textarea class="form-control" id="editReviewContent" name="contenuto" rows="4" required></textarea>
          </div>
          <div class="d-flex justify-content-end gap-2">
            <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Annulla</button>
            <button type="submit" class="btn btn-warning">
              <i class="bi bi-check-lg me-2"></i>Salva Modifiche
            </button>
          </div>
        </form>
        <div id="editReviewMsg" class="mt-2"></div>
      </div>
    </div>
  </div>
</div>

<!-- Modal conferma eliminazione recensione -->
<div class="modal fade" id="deleteReviewModal" tabindex="-1" aria-labelledby="deleteReviewModalLabel" aria-hidden="true">
  <div class="modal-dialog">
    <div class="modal-content">
      <div class="modal-header bg-danger text-white">
        <h5 class="modal-title" id="deleteReviewModalLabel">
          <i class="bi bi-exclamation-triangle me-2"></i>Conferma Eliminazione
        </h5>
        <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal" aria-label="Close"></button>
      </div>
      <div class="modal-body">
        <p>Sei sicuro di voler eliminare questa recensione? Questa azione non può essere annullata.</p>
        <div id="deleteReviewInfo" class="alert alert-warning">
          <strong>Recensione:</strong> <span id="deleteReviewTitle"></span>
        </div>
        <input type="hidden" id="deleteReviewId">
      </div>
      <div class="modal-footer">
        <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Annulla</button>
        <button type="button" class="btn btn-danger" id="confirmDeleteReview">
          <i class="bi bi-trash me-2"></i>Elimina Recensione
        </button>
      </div>
    </div>
  </div>
</div>

        <%- include('partials/footer') %>
        <script type="module">
            import Profilo from '/assets/scripts/components/Profilo.js';
            
            new Profilo(document.getElementById('page'));
        </script>
        <script>
          // Validazione telefono in tempo reale nel form modifica profilo
          document.addEventListener('DOMContentLoaded', function() {
            const telefonoInput = document.getElementById('editTelefono');
            if (telefonoInput) {
              // Auto-normalizza con +39
              telefonoInput.addEventListener('input', function() {
                let val = this.value.trim();
                // Auto-aggiungi +39 se l'utente inizia a digitare numeri
                if (val.length > 0 && !val.startsWith('+')) {
                  this.value = '+39' + val.replace(/^0/, '');
                }
                // Rimuovi caratteri non validi (permetti solo +, numeri e spazi)
                this.value = this.value.replace(/[^+0-9\\s]/g, '');
              });
              
              // Valida al blur
              telefonoInput.addEventListener('blur', function() {
                let val = this.value.trim();
                if (val && !val.startsWith('+39')) {
                  this.value = '+39' + val.replace(/^0/, '');
                }
                // Valida il formato
                const phoneRegex = /^\\+39\\s?[0-9]{9,10}$/;
                if (val && !phoneRegex.test(this.value)) {
                  this.classList.add('is-invalid');
                  this.classList.remove('is-valid');
                } else if (val) {
                  this.classList.remove('is-invalid');
                  this.classList.add('is-valid');
                }
              });
              
              // Pre-normalizza il valore esistente
              if (telefonoInput.value) {
                let tel = telefonoInput.value.trim();
                if (tel && !tel.startsWith('+39')) {
                  telefonoInput.value = '+39' + tel.replace(/^0/, '');
                }
              }
            }
          });
        </script>
        <script>
          // Show notification enable modal from Profile page
          (function(){
            function createNotificationModal() {
              if (document.getElementById('notificationModal')) return;
              const modalHtml = `
                <div class="modal fade" id="notificationModal" tabindex="-1" aria-labelledby="notificationModalLabel" aria-hidden="true" data-bs-backdrop="static" data-bs-keyboard="false">
                    <div class="modal-dialog modal-dialog-centered">
                        <div class="modal-content">
                            <div class="modal-header border-0">
                                <h5 class="modal-title fw-bold" id="notificationModalLabel">
                                    <i class="bi bi-bell-fill text-primary me-2"></i>Abilita le Notifiche
                                </h5>
                            </div>
                            <div class="modal-body text-center py-4">
                                <div class="mb-3">
                                    <i class="bi bi-calendar-check display-1 text-primary"></i>
                                </div>
                                <h6 class="mb-3">Resta aggiornato sulle tue prenotazioni!</h6>
                                <p class="text-muted mb-0">
                                    Riceverai notifiche quando:
                                </p>
                                <ul class="list-unstyled mt-3">
                                    <li class="mb-2"><i class="bi bi-check-circle-fill text-success me-2"></i>Una prenotazione viene confermata</li>
                                    <li class="mb-2"><i class="bi bi-x-circle-fill text-danger me-2"></i>Una prenotazione viene annullata</li>
                                    <li class="mb-2"><i class="bi bi-bell-fill text-info me-2"></i>Nuove comunicazioni importanti</li>
                                </ul>
                            </div>
                            <div class="modal-footer border-0 d-flex gap-2">
                                <button type="button" class="btn btn-outline-secondary flex-fill" id="skipNotifications">
                                    Più tardi
                                </button>
                                <button type="button" class="btn btn-primary flex-fill" id="enableNotifications">
                                    Abilita
                                </button>
                            </div>
                        </div>
                    </div>
                </div>
              `;
              document.body.insertAdjacentHTML('beforeend', modalHtml);
            }

            async function showProfileNotificationPrompt() {
              if (!('Notification' in window) || !('serviceWorker' in navigator)) {
                showNotificationResult('error', 'Browser non supportato', 'Il tuo browser non supporta le notifiche push.');
                return;
              }

              if (Notification.permission === 'granted') {
                // Verifica se già subscription esiste
                try {
                  const checkRes = await fetch('/push/my-subscriptions', { credentials: 'include' });
                  const checkData = await checkRes.json();
                  if (checkData.count > 0) {
                    showNotificationResult('info', 'Notifiche già abilitate', 'Hai già abilitato le notifiche per questo dispositivo.');
                    return;
                  }
                  // Non ha subscription, inizializza
                  if (window.initPushNotifications) {
                    await window.initPushNotifications();
                    showNotificationResult('success', 'Notifiche abilitate!', 'Riceverai notifiche per aggiornamenti importanti.');
                  }
                } catch (err) {
                  console.error('Errore verifica subscription:', err);
                  if (window.initPushNotifications) await window.initPushNotifications();
                }
                return;
              }
              if (Notification.permission === 'denied') {
                showNotificationResult('error', 'Permessi negati', 'Hai bloccato le notifiche. Abilitale nelle impostazioni del browser.');
                return;
              }

              createNotificationModal();
              const modalEl = document.getElementById('notificationModal');
              const bsModal = new bootstrap.Modal(modalEl);
              const enableBtn = document.getElementById('enableNotifications');
              const skipBtn = document.getElementById('skipNotifications');

              enableBtn.addEventListener('click', async () => {
                bsModal.hide();
                try {
                  if (window.initPushNotifications) {
                    await window.initPushNotifications();
                    showNotificationResult('success', 'Notifiche abilitate!', 'Riceverai notifiche per aggiornamenti importanti.');
                  }
                } catch (err) {
                  console.error('Errore attivazione notifiche:', err);
                  showNotificationResult('error', 'Errore attivazione', 'Si è verificato un errore. Riprova più tardi.');
                }
              }, { once: true });

              skipBtn.addEventListener('click', () => {
                bsModal.hide();
              }, { once: true });

              modalEl.addEventListener('hidden.bs.modal', function () { this.remove(); });
              bsModal.show();
            }
            
            function showNotificationResult(type, title, message) {
              const iconMap = { success: 'check-circle-fill', error: 'x-circle-fill', info: 'info-circle-fill' };
              const colorMap = { success: 'success', error: 'danger', info: 'primary' };
              const resultHtml = `
                <div class="modal fade" id="notificationResultModal" tabindex="-1" aria-hidden="true">
                  <div class="modal-dialog modal-dialog-centered">
                    <div class="modal-content">
                      <div class="modal-body text-center py-4">
                        <i class="bi bi-${iconMap[type]} text-${colorMap[type]}" style="font-size: 3rem;"></i>
                        <h5 class="mt-3 fw-bold">${title}</h5>
                        <p class="text-muted">${message}</p>
                        <button type="button" class="btn btn-${colorMap[type]} mt-2" data-bs-dismiss="modal">OK</button>
                      </div>
                    </div>
                  </div>
                </div>
              `;
              const existingModal = document.getElementById('notificationResultModal');
              if (existingModal) existingModal.remove();
              document.body.insertAdjacentHTML('beforeend', resultHtml);
              const modalEl = document.getElementById('notificationResultModal');
              const bsModal = new bootstrap.Modal(modalEl);
              modalEl.addEventListener('hidden.bs.modal', function () { this.remove(); });
              bsModal.show();
            }

            // Esponi dati utente globalmente per Profilo.js
            window.currentUser = {
              id: <%= user.id %>,
              nome: '<%= user.nome %>',
              cognome: '<%= user.cognome %>',
              email: '<%= user.email %>',
              telefono: '<%= user.telefono || "" %>',
              data_nascita: '<%= user.data_nascita || "" %>',
              codice_fiscale: '<%= user.codice_fiscale || "" %>',
              ruolo_preferito: '<%= user.ruolo_preferito || "" %>',
              piede_preferito: '<%= user.piede_preferito || "" %>'
            };

            document.addEventListener('DOMContentLoaded', function(){
              const btn = document.getElementById('enablePushBtn');
              if (btn) btn.addEventListener('click', showProfileNotificationPrompt);
              
              // Precompila TUTTI i campi del form profilo con i valori attuali
              const ruoloPreferito = '<%= user.ruolo_preferito || "" %>';
              const piedePreferito = '<%= user.piede_preferito || "" %>';
              
              if (ruoloPreferito) {
                const ruoloSelect = document.getElementById('ruoloPreferito');
                if (ruoloSelect) {
                  ruoloSelect.value = ruoloPreferito;
                }
              }
              
              if (piedePreferito) {
                const piedeSelect = document.getElementById('piedePreferito');
                if (piedeSelect) {
                  piedeSelect.value = piedePreferito;
                }
              }
            });
          })();
        </script>
        <script src="/assets/scripts/scroll-reveal.js"></script>
</body>
</html>